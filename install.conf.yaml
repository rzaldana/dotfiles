- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

# Update all submodules in dotfiles repo
- shell:
    - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.tmux.conf: tmux.conf

